# Lunar Crater Detection — LRO grayscale JPGs, Mask R-CNN, LoRA (fused-qkv)
# Backbone: lunarmind_v1_base pretrained on WAC vis + metadata.
#
# Based on crater_detection_lro_vis_maskrcnn_ml_sfp_dp01v2.yaml. Difference:
# instead of full fine-tune + LLRD, the pretrained encoder is frozen and only
# LoRA adapters (fused-qkv path) + detector heads (RPN, ROI, mask head) /
# neck / new-modality embedders train.
#
# See crater_detection_nac_only_lora_v2.yaml for the rationale on skipping
# replace_qkv.
#
# Requires: pip install terratorch[peft]
#
# RUN:
#   PYTHONPATH=.:$PYTHONPATH \
#   terratorch fit --config terratorch_integration/configs/lro_craters/crater_detection_lro_vis_maskrcnn_lora_v2.yaml
seed_everything: 42
custom_modules_path: terratorch_integration

trainer:
  accelerator: gpu
  devices: 1
  precision: "32"
  max_epochs: 100
  check_val_every_n_epoch: 1
  log_every_n_steps: 10
  enable_checkpointing: true
  logger:
    class_path: TensorBoardLogger
    init_args:
      save_dir: tb_logs/lro-crater
      name: crater-detection-lro-vis-maskrcnn-lora-v2
  callbacks:
    - class_path: LearningRateMonitor
      init_args:
        logging_interval: epoch
    - class_path: ModelCheckpoint
      init_args:
        dirpath: tb_logs/checkpoints/crater-detection-lro-vis-maskrcnn-lora-v2
        monitor: val_bbox_map
        mode: max
        filename: best-{epoch:02d}-{val_bbox_map:.3f}
        save_last: true
    - class_path: EarlyStopping
      init_args:
        monitor: val_bbox_map
        mode: max
        patience: 15

data:
  class_path: terratorch_integration.data_adapter.LunarCraterDataModule
  init_args:
    root: data/LRO_Craters
    batch_size: 4
    num_workers: 4
    image_size: 512
    output_mode: vis
    replicate_bands: 5
    norm_mean: 0.251561
    norm_std: 0.121082
    load_masks: true
    train_transforms:
      class_path: terratorch_integration.data_utils.D4DetectionTransform
      init_args:
        image_keys: [image, vis]
        p: 1.0

model:
  class_path: terratorch_integration.LunarObjectDetectionTask
  init_args:
    model_factory: ObjectDetectionModelFactory
    model_args:
      framework: mask-rcnn
      backbone: lunarmind_v1_base
      backbone_checkpoint_path: weights/backbone/checkpoint_weights_final.pt
      backbone_cfg: weights/backbone/full_config.yaml
      backbone_modality_info_path: weights/modality_info.yaml

      num_classes: 2
      framework_min_size: 512
      framework_max_size: 512

      backbone_modalities:
        - vis

      backbone_patch_size: 8
      backbone_remove_register_tokens: false
      backbone_merge_method: concat

      backbone_drop_path_rate_encoder: 0.1

      in_channels: 5
      necks:
        - name: SelectIndices
          indices: [2, 5, 8, 11]
        - name: ReshapeTokensToImage
          remove_cls_token: false
          h: 64
        - name: MultilayerSimpleFeaturePyramid
          out_channels: 256
        - name: FeaturePyramidNetworkNeck

    freeze_backbone: false
    freeze_decoder: false

    class_names:
      - Background
      - Crater

    anchor_sizes: [[8], [16], [32], [64]]
    anchor_aspect_ratios: [0.5, 1.0, 2.0]

    # -------------------- PEFT / LoRA (fused-qkv path) --------------------
    # target_modules includes mlp.fc3: TerraMind uses SwiGLU (GatedMlp) with
    # fc1/fc2/fc3 — fc3 is the gate branch, previously missing.
    # exclude_modules skips TerraMind's decoder blocks entirely (LunarBackbone
    # only forward-calls the encoder; decoder adapters would receive no
    # gradient signal but still cost memory + step time).
    peft_config:
      method: LORA
      peft_config_kwargs:
        r: 16
        lora_alpha: 32
        lora_dropout: 0.05
        bias: none
        target_modules:
          - qkv
          - mlp.fc1
          - mlp.fc2
          - mlp.fc3
        exclude_modules: '.*decoder\..*'

    # LoRA replaces LLRD (base encoder frozen; no pretrained-block params to
    # decay). Flat LR, layer_decay: 1.0, no WD on adapters.
    backbone_lr: 1.0e-4
    head_lr: 1.0e-4
    layer_decay: 1.0
    weight_decay: 0.0
    head_weight_decay: 1.0e-3
    betas: [0.9, 0.999]
    warmup_steps: 500
    eta_min: 1.0e-6

# NOTE: intentionally NO top-level optimizer / lr_scheduler blocks.
# Lightning CLI monkey-patches configure_optimizers when present, which
# would silently discard the param groups defined by the task.
