Planning
========

Refactor Checklist
------------------

1. Restructure the repository layout under lfm/.
   - Remove code duplication between semantic segmentation and instance
     segmentation where shared behavior is clear.
   - Reorganize the current lfm/tasks/ code into lfm/toy_model/.
   - Remove the lfm/tasks/ folder after the new structure is in place.
   - Ensure existing imports continue to work in notebooks and .py files after
     the move.
   - Target structure:
     - lfm/toy_model/all_tasks/: shared .py files for instance and semantic
       segmentation.
     - lfm/toy_model/sem_seg/: semantic segmentation .py files.
     - lfm/toy_model/inst_seg/: instance segmentation .py files.
     - lfm/full_model/: leave empty except for __init__.py.

2. Add support for using all static and all WAC bands.
   - Placeholder for follow-up design discussion.
   - Define exact expected band discovery, ordering, filtering, metadata, and
     model input behavior before implementation.
