[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "lfm"
version = "0.1.0"
description = "Lunar foundation model segmentation workflows"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["lfm*"]