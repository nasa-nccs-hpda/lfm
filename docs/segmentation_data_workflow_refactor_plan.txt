Segmentation Data And Workflow Refactor Plan
===========================================

Purpose
-------

This document captures a feasible refactor direction for reducing duplicated
semantic segmentation and instance segmentation workflow code while preserving
the repository's symmetry goals.

Core Refactor Constraints
------------------------

- Maintain Toy vs Graha symmetry whenever it does not reduce functionality.
- Maintain semantic segmentation vs instance segmentation symmetry whenever task
  differences do not require different structure.
- Avoid compatibility-only files. If code moves, update imports and references
  directly, then remove the obsolete location.
- Treat the eventual package structure as a three-axis separation:
  shared-across-models, Graha-specific, and Toy-specific.
- Use clean inheritance only at stable specialization boundaries, such as
  semantic-vs-instance target formatting.
- Prefer composition for behavior that varies independently, such as
  normalization, nodata handling, modality registration, plotting, checkpointing,
  trainer construction, and workflow orchestration.

Current Recommended Direction
-----------------------------

The overall direction is feasible: separate task data, model family behavior,
and experiment orchestration into explicit objects/modules instead of letting
scripts glue all behavior together.

The highest-value first target is the data layer, especially Toy semantic data
loading. This is where duplicated image loading, band filtering, normalization,
spatial transforms, label matching, and nodata behavior create avoidable churn.

Recommended Structure, Phased
-----------------------------

Eventual Target Package Structure
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

The long-term package target is:

`lfm/lfm/all_models/all_tasks/`
`lfm/lfm/all_models/sem_seg/`
`lfm/lfm/all_models/inst_seg/`

`lfm/lfm/graha_model/all_tasks/`
`lfm/lfm/graha_model/sem_seg/`
`lfm/lfm/graha_model/inst_seg/`

`lfm/lfm/toy_model/all_tasks/`
`lfm/lfm/toy_model/sem_seg/`
`lfm/lfm/toy_model/inst_seg/`

Ownership rules:

- `all_models/all_tasks`: shared across Toy and Graha, and shared across
  semantic segmentation and instance segmentation.
- `all_models/sem_seg`: shared across Toy and Graha, but specific to semantic
  segmentation.
- `all_models/inst_seg`: shared across Toy and Graha, but specific to instance
  segmentation.
- `graha_model/all_tasks`: shared across semantic and instance segmentation,
  but specific to Graha behavior.
- `toy_model/all_tasks`: shared across semantic and instance segmentation, but
  specific to Toy behavior.
- `graha_model/{sem_seg,inst_seg}` and `toy_model/{sem_seg,inst_seg}`:
  model-family-specific and task-specific implementation details.

This structure should be introduced gradually. Avoid creating empty mirrored
directories solely for symmetry. Prefer moving code when there is a concrete
shared behavior to extract.

Naming Convention
~~~~~~~~~~~~~~~~~

Use `Graha` as the canonical model-family name for the Lunar-FM/Graha path.
Avoid adding new names that use `full_model` or "full model" for repo-owned
code. Existing `lfm/full_model/...` paths are legacy naming and should be
renamed during phase 10 after behavior-bearing refactors are stable.

Conventions:

- User-facing text and docs: `Graha`, or `Graha/Lunar-FM` when the connection
  to the original model family needs to be explicit.
- Python package directories: `graha_model`, mirroring the existing
  `toy_model` package.
- Python classes and types: `Graha...`.
- Python functions, variables, config fields, and CLI flags: `graha_...` or
  `--graha-...`.
- Historical references may keep exact paths such as `lfm/full_model/...` when
  documenting current code or completed migrations, but those names should be
  treated as legacy.

The vendored `graha-lunar-fm` directory is a special case. Do not rename the
vendored repo itself unless that is planned separately. If the parent package is
renamed later, the likely shape is `lfm/graha_model/graha-lunar-fm/...`.

Object-Oriented Design Target
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

The desired design should use shallow inheritance for stable structural
boundaries and composition for behaviors that vary independently.

Inheritance should cover:

- Dataset shape:
  - `BaseLunarSegmentationDataset`
  - `SemanticSegmentationDataset`
  - `InstanceSegmentationDataset`
- DataModule shape:
  - `BaseLunarSegmentationDataModule`
  - `SemanticSegmentationDataModule`
  - `InstanceSegmentationDataModule`
- Workflow/model adapter shape, later:
  - `BaseWorkflow`
  - `ToyWorkflow`
  - `GrahaWorkflow`
  - `BaseModelAdapter`
  - `ToySemanticModelAdapter`
  - `GrahaSemanticModelAdapter`
  - `ToyInstanceModelAdapter`
  - `GrahaInstanceModelAdapter`

Composition should cover:

- `ImageReader`
- `LabelReader`
- `PairMatcher`
- `NormalizationStrategy`
- `NoDataPolicy`
- `SpatialTransform`
- `TargetFormatter`
- `CollatePolicy`
- `Plotter`
- `PredictionCache`
- `CheckpointPolicy`
- `TrainerFactory`

Avoid inheritance trees that combine independent axes. Do not create classes
such as `ToyNacSemanticDataset` or `GrahaWacInstanceDataset` unless there is a
real model-family-specific behavioral boundary that cannot be expressed through
composition.

The near-term data package target is:

`all_models/all_tasks/data/base_dataset.py`
`all_models/all_tasks/data/base_datamodule.py`
`all_models/all_tasks/data/image_io.py`
`all_models/all_tasks/data/normalization.py`
`all_models/all_tasks/data/nodata.py`
`all_models/all_tasks/data/image_crop_resize.py`
`all_models/all_tasks/data/tensor_utils.py`
`all_models/all_tasks/data/collate.py`

`all_models/sem_seg/semantic_dataset.py`
`all_models/sem_seg/semantic_datamodule.py`

`all_models/inst_seg/instance_dataset.py`
`all_models/inst_seg/instance_datamodule.py`
`all_models/inst_seg/instance_data_utils.py`

Phase 1: Shared Data And Preprocessing

Shared data utilities now live under:

`lfm/all_models/all_tasks/data/`

They were initially introduced under `lfm/full_model/all_tasks/data/`, but that
temporary location should not be used going forward. Shared Toy/Graha imports
should use `lfm.all_models.all_tasks.data`.

Candidate modules:

- `image_io.py`
- `normalization.py`
- `nodata.py`
- `base_dataset.py`
- `base_datamodule.py`

Responsibilities:

- Match image and label files.
- Read image files consistently.
- Apply band filtering.
- Apply crop/resize transforms.
- Apply nodata policy.
- Apply normalization strategy.

Sub-steps:

1. [Completed] Move the new shared data scaffold from the temporary location:

   `lfm/full_model/all_tasks/data/`

   to the final shared-across-models location:

   `lfm/all_models/all_tasks/data/`

2. [Completed] Update imports directly. Do not leave compatibility modules in
   `lfm/full_model/all_tasks/data`.

3. [Completed] Split or merge overlapping utility behavior from
   `lfm/full_model/all_tasks/datamodules/datamodule_utils.py` into the new data
   package:

   - File matching and image IO belong in `image_io.py`.
   - Tensor conversion belongs in `tensor_utils.py`.
   - Generic crop helpers belong in `image_crop_resize.py`.
   - Semantic/instance collate functions belong in `collate.py`.
   - Instance box/mask target helpers belong in
     `all_models/inst_seg/instance_data_utils.py`.

4. [Completed] Keep the first move narrow. Prefer moving utilities that are
   already used by both Toy and Graha before migrating larger dataset/datamodule
   classes.

Phase 2: Shared Dataset Path

Introduce a shared segmentation dataset base:

`BaseLunarSegmentationDataset`

This should handle image-side behavior once. Semantic and instance datasets
should only specialize target formatting:

- Semantic target output: `{"image", "mask", "filename"}`
- Instance target output: `{"image", "boxes", "masks", "labels", ...}`

Use inheritance only for the semantic-vs-instance target formatting boundary.
Use composition for behavior that varies independently.

Sub-steps:

1. Define `BaseLunarSegmentationDataset`.

   It should own the common sample lifecycle:

   - pair image and label paths
   - read image data
   - read raw label data
   - convert image layout
   - apply band filtering
   - apply nodata image policy
   - apply normalization strategy
   - apply spatial transform
   - return a prepared sample dict

   It should not decide final semantic-vs-instance target structure.

2. Define `SemanticSegmentationDataset(BaseLunarSegmentationDataset)`.

   It should specialize:

   - semantic label validation
   - semantic mask conversion
   - semantic ignore-index handling
   - semantic output formatting

3. [Completed] Define `InstanceSegmentationDataset(BaseLunarSegmentationDataset)`.

   It should specialize:

   - `.npz` instance label validation
   - instance mask conversion
   - box conversion/cropping
   - object-detection target formatting
   - instance output formatting

   `lfm.all_models.inst_seg.InstanceSegmentationDataset` now owns the shared
   split instance-label loading path used by Toy Mask2Former and Toy Mask R-CNN.
   It currently inherits from the all-task `LunarSegmentationDataset` base; the
   planned `BaseLunarSegmentationDataset` rename remains a later naming cleanup.

4. [Completed] Migrate Toy semantic first.

   The existing `LunarCraterDataset` can either become a thin subclass of
   `SemanticSegmentationDataset` if its name must remain temporarily, or be
   replaced directly if all imports can be updated in one step.

5. [Completed] Migrate Toy instance second.

   This is higher risk than semantic because it needs box/mask target behavior
   and object-detection collate semantics.

   Toy instance datamodules now instantiate
   `lfm.all_models.inst_seg.InstanceSegmentationDataset`, and Toy Mask R-CNN
   subclasses it for object-detection target formatting.

6. [Completed] Extract shared Toy instance datamodule mechanics.

   `lfm.all_models.inst_seg.data.instance_datamodule.InstanceSegmentationDataModule`
   now owns the duplicated split setup, dataset construction, optional train
   statistics, and dataloader creation used by the Toy Mask2Former and Toy
   Mask R-CNN instance datamodules. The Toy classes remain real
   specializations: they supply Toy input metadata behavior, and Mask R-CNN
   supplies its object-detection dataset, collate function, and stats key.

7. [Completed] Align Toy wrapper split layout and nodata composition.

   Toy Lightning wrapper data paths should use the same pre-split folder
   structure expected by the repo-owned full-model/Graha datamodules:

   `data_root/train/chips`
   `data_root/train/labels`
   `data_root/val/chips`
   `data_root/val/labels`
   `data_root/test/chips`
   `data_root/test/labels`

   Keep older legacy helper code stable, but route active Toy wrapper paths
   through composed `NoDataPolicy` behavior where they use shared datasets.
   Toy semantic already used this split-folder wrapper shape. Toy instance now
   validates the same split-folder structure, threads `NoDataPolicy` through
   the shared instance datamodule, applies nodata-aware image/mask handling in
   the shared instance dataset, and skips non-positive instance IDs when
   building Mask2Former targets.

8. [Completed] Review Graha semantic and instance datasets after Toy paths are stable.

   Do not touch `lfm/full_model/graha-lunar-fm`. Only migrate repo-owned Graha
   wrapper/datamodule code where behavior clearly overlaps.

   Repo-owned full-model datamodules now compose shared all-model helpers for
   split-folder layout resolution and nodata-policy construction while
   preserving TerraTorch-specific output contracts and legacy flat-layout
   support. Full inheritance from the `all_models` dataset/datamodule base is
   intentionally deferred until the shared base can express both Toy and Graha
   constructor/output contracts without forcing task-specific behavior into the
   wrong layer.

Phase 3: Normalization Strategy Objects

Introduce composable normalization strategies:

- `NoNormalization`
- `FinetuneStatsNormalization`
- `PretrainYamlNormalization`

Both Toy and Graha workflows should request normalization through the same
interface. The strategy can hide whether stats come from train-split data,
TerraMind pretraining YAML, or no normalization.

Sub-steps:

1. [Completed] Keep the shared interface simple:

   `NormalizationStrategy.apply(image, *, band_filter) -> image`

   The strategy interface also supports CHW tensors through
   `NormalizationStrategy.apply_tensor(image, *, band_filter=None) -> image` so
   older datamodule paths can use the same composition object before all dataset
   classes are unified.

2. [Completed] Use concrete strategy objects:

   - `NoNormalization`
   - `FinetuneStatsNormalization`
   - `PretrainYamlNormalization`

3. [Completed] Move ad hoc normalization calls out of dataset/datamodule code
   as each path migrates.

   The current Toy semantic, Toy instance, and repo-owned Graha datamodule paths
   now compose a normalization strategy while preserving existing `means`/`stds`
   constructor arguments.

4. [Completed] Keep modality-specific stats selection outside the dataset. The
   workflow or model adapter should decide which normalization strategy to pass
   in.

   TerraMind pretraining-stat loading now lives with shared normalization code
   under `lfm.all_models.all_tasks.data.normalization`, and active Toy/Graha
   workflows import it directly from that shared layer. The full-model utility
   package re-exports the same functions for older callers, but no shared
   `all_models` normalization code depends on `full_model`.

Phase 4: Shared Nodata Policy

Introduce a shared nodata preprocessing policy, for example:

`NoDataPolicy(ignore_in_loss=True, ignore_index=-1, image_fill_value=0.0)`

The dataset should apply image-side nodata behavior once. Losses and metrics
should consume the same `ignore_index` so masking semantics stay consistent.

Sub-steps:

1. [Completed] Keep nodata detection in image IO or preprocessing utilities.

2. [Completed] Keep nodata decisions in `NoDataPolicy`:

   - whether nodata pixels should be ignored in loss
   - which target ignore index to use
   - which image fill value to use

   `NoDataPolicy` now supports both HWC numpy arrays and CHW/HW torch tensor
   paths so the older repo-owned full-model datamodules and newer shared data
   scaffold use the same policy object.

3. [Completed] Pass the same ignore index to loss and metric code.

   Semantic workflow config already threads `ignore_nodata_in_loss` and
   `nodata_ignore_index` into datamodules, losses, and metrics. Instance
   comparison, finetuning, and checkpoint-sweep entry points now expose the
   same config fields and pass them into Toy and Graha datamodules. Instance
   object-detection losses do not consume a semantic-style ignore index
   directly, but the shared target preprocessing receives the same nodata
   policy.

4. [Completed] Verify semantic and instance behavior separately.

   Dataset audits passed for both semantic and instance segmentation, and the
   smoke test passed after the shared nodata-policy threading changes. Instance
   segmentation keeps task-specific target handling while sharing image-side
   nodata behavior.

Phase 5: Move Toy Semantic Data Loading First

The practical first implementation step should be:

Consolidate Toy semantic data loading onto the shared
`SemanticSegmentationDataset` path.

This is intentionally narrow and testable. It should reduce duplicated code
without forcing a full experiment-runner rewrite.

After Toy semantic data loading is stable, mirror the same structure for:

- Graha semantic data loading where applicable.
- Toy instance data loading. [Completed]
- Graha instance data loading where applicable.

Sub-steps:

1. [Completed] Finish the current Toy semantic migration by moving shared imports from
   `full_model` to `all_models`.

2. [Completed] Replace legacy helper calls in Toy semantic with the new composed objects
   wherever behavior overlaps.

   Toy semantic now inherits from
   `lfm.all_models.sem_seg.SemanticSegmentationDataset`, which wraps the
   all-task image-side dataset preparation with a semantic target/output
   boundary.

3. [Completed] Keep the existing public datamodule/workflow behavior stable.

   `LunarCraterDataset` keeps its historical tuple output by implementing the
   shared `format_output` hook.

4. [Completed] Add or run smoke checks for:

   - dataset construction
   - one sample load
   - dataloader batch construction
   - optional nodata ignore behavior
   - optional normalization behavior

   Semantic and instance dataset audits passed with no behavioral differences,
   and the smoke test passed after the shared data-layer changes.

Phase 6: Model Adapters, Later

After the data layer stabilizes, consider model adapters:

- `ToySemanticModelAdapter`
- `GrahaSemanticModelAdapter`
- `ToyInstanceModelAdapter`
- `GrahaInstanceModelAdapter`

Adapters should know how to build model/task/trainer-related pieces. They
should not own file matching, image loading, target loading, or normalization
mechanics.

Sub-steps:

1. [Completed] Define a small model adapter interface only after the data layer is stable.

   Candidate responsibilities:

   - build model
   - build Lightning task/wrapper
   - build callbacks
   - expose model-family display name
   - expose checkpoint-loading behavior

   `lfm.all_models.all_tasks.model_adapters` now defines lightweight
   `ModelAdapter`, `SemanticModelAdapter`, and `InstanceModelAdapter` protocols
   without importing model-framework dependencies.

2. [Completed] Keep task differences explicit. A semantic adapter and instance adapter may
   share a base class, but should not hide task-specific loss/metric behavior.

   Semantic now has separate concrete adapters for Toy/DINO and Graha/Lunar-FM:

   - `lfm.toy_model.sem_seg.semantic_model_adapter.ToySemanticModelAdapter`
   - `lfm.full_model.sem_seg.semantic_model_adapter.GrahaSemanticModelAdapter`

   These adapters wrap existing workflow construction functions and keep
   model-family-specific behavior in the model-family packages.

3. [Completed] Use adapters from workflows, not from datasets/datamodules.

   Semantic comparison, semantic single-finetune, and semantic checkpoint sweep
   now invoke Toy and Graha workflow setup through concrete semantic adapters.
   Instance comparison, instance single-finetune, and instance checkpoint sweep
   now mirror the same structure through concrete instance adapters:

   - `lfm.toy_model.inst_seg.instance_model_adapter.ToyInstanceModelAdapter`
   - `lfm.full_model.inst_seg.instance_model_adapter.GrahaInstanceModelAdapter`

Phase 7: Experiment Runners, Later

Only after data and workflow boundaries are cleaner, consider:

- `SingleModelExperiment`
- `ComparisonExperiment`
- `CheckpointSweepExperiment`

At that point, scripts can become thin CLI wrappers that build config and call
experiment objects.

Sub-steps:

1. [Completed] Extract repeated single-model run behavior into
   `SingleModelExperiment`.

   `lfm.all_models.all_tasks.experiments.SingleModelExperiment` now owns the
   shared one-model orchestration mechanics used by semantic and instance
   single-finetune wrappers:

   - create the output directory
   - create requested checkpoint subdirectories
   - save the model-specific config JSON
   - run the selected Toy or Graha workflow callable
   - write simple elapsed timing, or delegate to task-specific timing hooks

2. [Completed] Extract Toy-vs-Graha orchestration into `ComparisonExperiment`.

   `lfm.all_models.all_tasks.experiments.ComparisonExperiment` now owns the
   shared paired-model orchestration used by semantic and instance comparison
   scripts:

   - create the timestamped output directory supplied by the script
   - create requested checkpoint subdirectories
   - save the shared comparison config JSON
   - run Toy first, then Graha
   - pass both workflow results to task-specific completion hooks for plotting,
     metrics, and timing

3. [Completed] Extract repeated checkpoint enumeration/evaluation into
   `CheckpointSweepExperiment`.

   `lfm.all_models.all_tasks.experiments.CheckpointSweepExperiment` now owns the
   shared checkpoint-sweep orchestration used by semantic and instance
   checkpoint sweep scripts:

   - create the output root
   - seed through the script-provided Lightning seed function
   - validate model-specific checkpoint directories
   - discover, sort, de-duplicate, and limit checkpoint records
   - dispatch Toy and Graha checkpoint sweeps in the requested order

   Shared checkpoint utilities now also own Lightning checkpoint state loading
   and checkpoint metrics summary writing. Task-specific scripts still own
   semantic-vs-instance evaluation, prediction/cache output details, and metric
   definitions.

4. [Completed] Audit current workflow modules and classify responsibilities.

   The goal is not to move all `*_finetuning.py` code wholesale into
   `scripts/python`. Those modules currently mix reusable model-family
   construction behavior with driver/orchestration behavior.

   Classify each function in the current Toy and Graha workflow modules as one
   of:

   - reusable package behavior, which should remain under `lfm/`
   - script-level driver behavior, which should move under `scripts/python/`
   - task-specific output/metric behavior, which may remain script-local until
     it has clear reuse pressure

   Package behavior includes:

   - model/task construction
   - datamodule construction
   - normalization-stat selection
   - TerraTorch/Graha dependency import and path setup
   - callback factories that are reusable outside one script

   Driver behavior includes:

   - running fit/test/predict workflows
   - choosing output directories
   - saving script-run timing/config artifacts
   - invoking prediction cache generation
   - invoking comparison/evaluation/reporting steps

5. [Completed] Rename or split package modules so `lfm/full_model/{task}` no
   longer reads like a script location.

   The Graha package should continue to own Graha reusable implementation
   details, but files named like standalone training scripts are misleading
   once orchestration lives under `scripts/python`. The current package path is
   still the legacy `lfm/full_model/{task}` name until phase 10 renames it.

   Candidate package-module shape:

   - `lfm/full_model/sem_seg/semantic_model_adapter.py`
   - `lfm/full_model/sem_seg/semantic_graha_components.py`
   - `lfm/full_model/inst_seg/instance_model_adapter.py`
   - `lfm/full_model/inst_seg/instance_graha_components.py`

   Equivalent Toy reusable components should remain under:

   - `lfm/toy_model/sem_seg/`
   - `lfm/toy_model/inst_seg/`

   Do not create compatibility-only modules. If a file is renamed or split,
   update imports directly and remove the obsolete file once no code references
   it.

   The package-level `*_finetuning.py` modules have been renamed to component
   modules and no longer own top-level CLI driver code:

   - `lfm/full_model/sem_seg/semantic_graha_components.py`
   - `lfm/full_model/inst_seg/instance_graha_components.py`
   - `lfm/toy_model/sem_seg/semantic_toy_components.py`
   - `lfm/toy_model/inst_seg/instance_toy_components.py`

   The legacy Graha-only `*_finetuning.py` script entry points were removed.
   Use the maintained single-model entry points with `--model graha` for
   Graha-only fine-tuning:

   - `scripts/python/semantic_seg/semantic_seg_single_finetune.py`
   - `scripts/python/instance_seg/instance_seg_single_finetune.py`
   - `scripts/shell/semantic_seg/sbatch_semantic_seg_single_finetune.sh`
   - `scripts/shell/instance_seg/sbatch_instance_seg_single_finetune.sh`

6. [Completed] Move script-level Toy and Graha workflow orchestration under
   `scripts/python/{task}`.

   Candidate script-level workflow modules:

   - `scripts/python/semantic_seg/semantic_toy_workflow.py`
   - `scripts/python/semantic_seg/semantic_graha_workflow.py`
   - `scripts/python/instance_seg/instance_toy_workflow.py`
   - `scripts/python/instance_seg/instance_graha_workflow.py`

   These files may call package-level adapters/components from `lfm/`, but
   package modules under `lfm/` should not import from `scripts/python`.

   Keep the symmetry rule explicit:

   - if a Toy workflow helper exists for semantic segmentation, the Graha
     semantic workflow should expose an equivalent helper when feasible
   - if semantic segmentation has a workflow module shape, instance
     segmentation should mirror that shape unless task-specific behavior makes
     the split misleading

   Semantic progress:

   - `scripts/python/semantic_seg/semantic_toy_workflow.py` now owns Toy
     semantic fit/cache/test orchestration.
   - `scripts/python/semantic_seg/semantic_graha_workflow.py` now owns Graha
     semantic fit/cache orchestration.
   - Semantic comparison and semantic single-finetune entry points call those
     script-level workflows directly.
   - Semantic model adapters no longer expose `run_workflow`; they remain
     focused on reusable construction helpers.

   Instance progress:

   - `scripts/python/instance_seg/instance_toy_workflow.py` now owns Toy
     instance fit/cache/plot orchestration.
   - `scripts/python/instance_seg/instance_graha_workflow.py` now owns Graha
     instance fit/cache/plot orchestration.
   - Instance comparison and instance single-finetune entry points call those
     script-level workflows directly.
   - Instance model adapters no longer expose `run_workflow`; they remain
     focused on reusable construction helpers.

7. [Completed] Thin the top-level CLI entry points.

   The scripts below should become mostly CLI wrappers:

   - `scripts/python/semantic_seg/semantic_seg_comparison.py`
   - `scripts/python/semantic_seg/semantic_seg_single_finetune.py`
   - `scripts/python/semantic_seg/semantic_checkpoint_sweep.py`
   - `scripts/python/instance_seg/instance_seg_comparison.py`
   - `scripts/python/instance_seg/instance_seg_single_finetune.py`
   - `scripts/python/instance_seg/instance_checkpoint_sweep.py`
   - `scripts/python/all_tasks/checkpoint_pipeline.py`

   Their intended responsibilities are:

   - parse arguments
   - build config
   - call experiment object
   - exit with clear status

   Shared orchestration should live in experiment objects. Model-family
   construction should live in adapters/components. Task-specific workflow
   steps should live in importable package workflow modules under `lfm/`, not
   in `scripts/python`.

   Move the current workflow modules as part of this CLI-thinning step:

   - `scripts/python/semantic_seg/semantic_toy_workflow.py` to
     `lfm/all_models/sem_seg/workflows/semantic_toy_workflow.py`
   - `scripts/python/semantic_seg/semantic_graha_workflow.py` to
     `lfm/all_models/sem_seg/workflows/semantic_graha_workflow.py`
   - `scripts/python/instance_seg/instance_toy_workflow.py` to
     `lfm/all_models/inst_seg/workflows/instance_toy_workflow.py`
   - `scripts/python/instance_seg/instance_graha_workflow.py` to
     `lfm/all_models/inst_seg/workflows/instance_graha_workflow.py`

   Do not leave compatibility files under `scripts/python`; update imports
   directly.

   Progress:

   - Semantic epoch test-suite callback/metric/plot helpers moved out of
     `scripts/python/semantic_seg/semantic_seg_comparison.py`.
   - Instance epoch test-suite and Graha validation plot callbacks moved out of
     `scripts/python/instance_seg/instance_seg_comparison.py`.
   - Semantic and instance single-finetune wrappers now import those callbacks
     from reusable task helper modules instead of reaching through comparison
     entry-point modules.
   - Semantic and instance workflow orchestration modules moved from
     `scripts/python/{task}` to `lfm/all_models/{task}/workflows`.
   - Comparison and single-finetune entry points now import workflow modules
     from `lfm.all_models.{task}.workflows`.

8. [Completed] Move reusable test-suite callbacks and implementations under
   `lfm/all_models/{task}` with mirrored semantic/instance filenames.

   Callbacks are reusable training components rather than executable scripts,
   so they should live under the package layer. The symmetric target shape is:

   - `lfm/all_models/sem_seg/semantic_test_suite.py`
   - `lfm/all_models/sem_seg/semantic_test_suite_callback.py`
   - `lfm/all_models/inst_seg/instance_test_suite.py`
   - `lfm/all_models/inst_seg/instance_test_suite_callback.py`

   Import callbacks from the concrete callback modules rather than from the
   package `__init__.py`, so ordinary dataset imports do not eagerly import
   Lightning or plotting/cache dependencies.

   The callback files should stay thin:

   - decide whether the current epoch should run the suite
   - call the task test-suite implementation
   - report the resulting summary

   The test-suite files should own reusable task behavior:

   - select the requested split/dataloader or prediction cache path
   - run/cache predictions
   - write per-sample arrays and aggregate metrics
   - create test-suite plots
   - provide checkpoint-sweep output/metric writers when the behavior is the
     same as test-suite output generation

   Do not leave script-local compatibility helper files. Update imports to the
   package modules directly and delete obsolete script helper files.

   Progress:

   - Semantic and instance callback files now live under `lfm/all_models`.
   - Semantic and instance comparison/single-finetune scripts import callbacks
     from the package modules.
   - Semantic checkpoint sweep now reuses
     `lfm/all_models/sem_seg/semantic_test_suite.py` for checkpoint output and
     metrics.
   - Instance checkpoint sweep now reuses
     `lfm/all_models/inst_seg/instance_test_suite.py` for checkpoint output and
     metrics.

9. [Completed] Move shared plotting/prediction-cache/evaluation utilities out of the
   misleading `lfm/full_model/all_tasks/utils` namespace when they are used by
   both Toy and Graha paths.

   Current mismatch:

   - `lfm/full_model/all_tasks/utils` contains utilities that are not
     full-model/Graha-specific.
   - Examples include prediction cache writing/reading, plotting helpers, and
     comparison/evaluation helpers used by Toy and Graha workflows.
   - Moving workflow modules under `lfm/all_models/{task}/workflows` will make
     this mismatch more visible because all-model workflow code will still
     import shared behavior from a `full_model` namespace.

   Target direction:

   - Move truly shared utilities to `lfm/all_models/all_tasks/utils` or
     task-specific `lfm/all_models/{task}` modules.
   - Keep Graha-only utilities under the Graha package, currently
     `lfm/full_model` and eventually `lfm/graha_model`.
   - Keep Toy-only utilities under `lfm/toy_model`.
   - Preserve existing behavior by updating imports directly; do not create
     compatibility-only modules.

   Progress:

   - Shared all-task utility modules moved to
     `lfm/all_models/all_tasks/utils`.
   - Shared semantic plotting moved to
     `lfm/all_models/sem_seg/semantic_plotting.py`.
   - Shared instance plotting and prediction-cache modules moved to
     `lfm/all_models/inst_seg`.
   - The semantic prediction-cache compatibility-only file was removed because
     semantic cache behavior is already covered by the shared all-task cache
     implementation.
   - Scripts, workflows, components, and notebooks were updated to import the
     new package locations directly.

10. [Completed] Validate the script cleanup without changing behavior.

   Validation should include:

   - import/syntax checks for touched modules
   - direct script `--help` checks where local dependencies permit
   - semantic and instance smoke tests
   - checkpoint sweep smoke tests for both semantic and instance segmentation
   - a structural scan confirming no `lfm/` package module imports from
     `scripts/python`

   Progress:

   - Import, syntax, formatting, and direct CLI `--help` checks passed for the
     touched phase 7 modules and entry points.
   - Semantic and instance smoke tests passed after the phase 7 utility
     namespace cleanup.
   - Checkpoint sweep smoke tests passed for both semantic and instance
     segmentation.
   - The active package code was scanned to confirm `lfm/` modules do not
     import from `scripts/python`.

Phase 8: Datamodule Inheritance

Review and tighten the dataset/datamodule inheritance and composition structure
now that workflows, callbacks, plotting, prediction cache, normalization, and
nodata policy are in their intended package layers.

Goal:

- Use inheritance only for stable structural boundaries.
- Use composition for independently varying behavior.
- Keep Toy vs Graha and semantic vs instance symmetry where it does not distort
  the task-specific output contracts.

Sub-steps:

1. Audit current dataset/datamodule classes and draw the real inheritance graph.

   Include:

   - `lfm/all_models/all_tasks/data/base_dataset.py`
   - `lfm/all_models/all_tasks/data/base_datamodule.py`
   - `lfm/all_models/sem_seg/semantic_dataset.py`
   - `lfm/all_models/inst_seg/instance_dataset.py`
   - `lfm/all_models/inst_seg/instance_datamodule.py`
   - Toy semantic and instance Lightning wrapper datamodules
   - repo-owned full-model/Graha semantic and instance datamodules

2. Identify behavior that should be inherited versus composed.

   Inheritance candidates:

   - split-folder dataset/datamodule lifecycle
   - semantic target formatting
   - instance target formatting
   - object-detection target formatting

   Composition candidates:

   - file matching
   - image reading
   - label reading
   - crop/resize policy
   - normalization
   - nodata policy
   - collate policy
   - train-stat calculation

3. Align semantic datamodule structure with instance datamodule structure where
   the shared shape is real.

   Avoid creating mirrored files that contain no behavior. If semantic does not
   need a concrete shared datamodule yet, document why and defer it rather than
   adding a compatibility-only module.

4. Evaluate whether repo-owned Graha datamodules should inherit from the shared
   all-model datamodule base or continue composing shared helpers.

   Criteria for inheritance:

   - constructor contracts can be made compatible without awkward adapter
     parameters
   - TerraTorch output dictionaries remain unchanged
   - flat-layout legacy support can either be preserved cleanly or deliberately
     removed in phase 9
   - semantic and instance paths can be kept structurally similar

5. Implement the smallest inheritance cleanup that removes real duplication.

   Do not force Graha datamodules into the shared base if composition remains
   clearer for TerraTorch-specific output contracts.

6. Validate with dataset audits and semantic/instance smoke tests.

Progress:

- Added `LunarSegmentationDataModule` under
  `lfm/all_models/all_tasks/data/base_datamodule.py` as the shared
  pre-split LightningDataModule lifecycle for active Toy/shared data paths.
- `LunarSegmentationDataModule` now owns:

  - `train/val/test/{chips,labels}` validation
  - optional input metadata setup
  - optional train-stat aggregation
  - train/val/test dataset creation lifecycle
  - standard dataloader construction
  - hook points for task-specific file-list/sanity output

- `lfm/all_models/inst_seg/instance_datamodule.py` now inherits from
  `LunarSegmentationDataModule` and keeps only instance-specific dataset
  construction, collate policy, and normalization-stat storage.
- `lfm/toy_model/sem_seg/lightning_wrappers/toy_sem_seg_datamodule.py` now
  inherits from `LunarSegmentationDataModule` and keeps Toy semantic-specific
  dataset construction, tuple-output behavior, file-list output, and sanity
  summaries.
- `lfm/toy_model/sem_seg/lightning_wrappers/toy_sem_seg_from_instance_datamodule.py`
  now declares only its dataset class, collate function, stats label, and
  semantic-from-instance sanity summary instead of duplicating dataloader and
  train-stat code.
- Repo-owned Graha datamodules intentionally remain under
  `lfm/full_model/all_tasks/datamodules.LunarSegmentationDatamodule` for now.
  That class still supports both split and flat layouts and preserves
  TerraTorch-specific output contracts. Full inheritance into the all-model
  split datamodule base should wait until phase 9 decides whether flat-layout
  support is deprecated.
- Local validation completed for the touched modules with compile and lint
  checks. Dataset audits and semantic/instance smoke tests passed, so the first
  phase 8 inheritance cleanup slice is complete.

Next Phase 8 Slice: Unify Graha Dataset/Datamodule Inheritance

Reason:

- The current tree is cleaner internally, but still transitional. Toy/shared
  paths use `lfm/all_models/all_tasks/data.LunarSegmentationDataModule` and
  `lfm/all_models/all_tasks/data.LunarSegmentationDataset`, while Graha paths
  still use a separate repo-owned base line under
  `lfm/full_model/all_tasks/datamodules`.
- This creates two competing base names and two datamodule lifecycles:

  - `lfm/all_models/all_tasks/data/base_dataset.py::LunarSegmentationDataset`
  - `lfm/full_model/all_tasks/datamodules/lunar_segmentation_dataset.py::LunarSegmentationDataset`
  - `lfm/all_models/all_tasks/data/base_datamodule.py::LunarSegmentationDataModule`
  - `lfm/full_model/all_tasks/datamodules/lunar_segmentation_datamodule.py::LunarSegmentationDatamodule`

- The target design is one shared dataset/datamodule inheritance tree, with
  Toy and Graha represented as model-family-specific specializations only where
  they genuinely need different output contracts.

Target inheritance shape:

```text
Dataset
└── LunarSegmentationDataset
    ├── SemanticSegmentationDataset
    │   ├── ToySemanticSegmentationDataset
    │   ├── GrahaSemanticSegmentationDataset
    │   └── GrahaSemanticFromInstanceDataset
    └── InstanceSegmentationDataset
        ├── ToyInstanceSegmentationDataset
        ├── ToyObjectDetectionInstanceDataset
        ├── GrahaInstanceSegmentationDataset
        └── GrahaObjectDetectionInstanceDataset

LightningDataModule
└── LunarSegmentationDataModule
    ├── SemanticSegmentationDataModule
    │   ├── ToySemanticSegmentationDataModule
    │   ├── GrahaSemanticSegmentationDataModule
    │   └── GrahaSemanticFromInstanceDataModule
    └── InstanceSegmentationDataModule
        ├── ToyInstanceSegmentationDataModule
        ├── ToyObjectDetectionInstanceDataModule
        ├── GrahaInstanceSegmentationDataModule
        └── GrahaObjectDetectionInstanceDataModule
```

Naming target:

- Use `LunarSegmentationDataset` and `LunarSegmentationDataModule` for the
  shared all-model base classes.
- Use `SemanticSegmentationDataset` / `SemanticSegmentationDataModule` and
  `InstanceSegmentationDataset` / `InstanceSegmentationDataModule` for
  task-level shared bases.
- Use `Toy*` and `Graha*` only at model-family specialization boundaries.
- Avoid `full_model` in new class/file names when the behavior is specifically
  Graha behavior. Existing package paths may remain until phase 10 naming
  cleanup decides whether to rename package directories.

Sub-steps for this slice:

7. [Completed] Create the shared semantic datamodule base.

   Add `lfm/all_models/sem_seg/semantic_datamodule.py` with a real
   `SemanticSegmentationDataModule` subclass of the shared all-task
   datamodule lifecycle.

   Responsibilities:

   - own semantic-specific dataset construction
   - set semantic collate policy
   - set semantic train-stat image key/log label
   - support semantic-from-instance as a real subclass hook, not as a
     compatibility file

   Do not create this file as a no-op mirror. It must own shared semantic
   datamodule behavior used by at least Toy and/or Graha code after this slice.

   Completed:

   - added `lfm/all_models/sem_seg/semantic_datamodule.py`
   - added `SemanticSegmentationDataModule` as the shared semantic split
     datamodule subclass of `LunarSegmentationDataModule`
   - moved Toy semantic split lifecycle, dataset construction, train-stat
     handling, file-list writing, and sanity-summary behavior into the shared
     semantic datamodule
   - reduced
     `lfm/toy_model/sem_seg/lightning_wrappers/toy_sem_seg_datamodule.py` to
     Toy-specific dataset and metadata specialization
   - kept
     `lfm/toy_model/sem_seg/lightning_wrappers/toy_sem_seg_from_instance_datamodule.py`
     inheriting through the Toy semantic datamodule while preserving its
     instance-label sanity summary override

8. [Completed] Move Graha semantic dataset/datamodule classes onto the shared
   semantic base.

   Convert the current Graha semantic classes from:

   - `lfm/full_model/sem_seg/semantic_mask_datamodule.py`
   - `lfm/full_model/sem_seg/semantic_from_instance_datamodule.py`

   into Graha-specific specializations that inherit from the shared semantic
   dataset/datamodule classes.

   Preserve:

   - output dictionary keys expected by TerraTorch tasks
   - semantic mask behavior
   - semantic-from-instance binarization behavior
   - nodata policy behavior
   - normalization behavior
   - split-folder data layout

   Decide explicitly whether flat-layout support remains. If it is no longer
   needed for active experiments, move flat-layout removal into phase 9
   deprecation rather than preserving it awkwardly in the new base.

   Completed:

   - `LunarSemanticMaskSegmentationDataset` now inherits from
     `lfm.all_models.sem_seg.SemanticSegmentationDataset`
   - `LunarSemanticMaskSegmentationDatamodule` now inherits from
     `lfm.all_models.sem_seg.SemanticSegmentationDataModule`
   - `LunarSemanticFromInstanceDataset` now inherits through the Graha semantic
     mask dataset and preserves crater box / crater count metadata
   - `LunarSemanticFromInstanceDatamodule` now inherits through the Graha
     semantic mask datamodule and keeps its instance-aware collate policy
   - semantic Graha datamodule code no longer imports the removed full-model
     all-task datamodule base package

   Deliberate deferral:

   - The Graha semantic classes still live under `lfm/full_model/sem_seg` for
     this slice to avoid a broad import-path churn in the same change. Later
     substeps update active imports and remove the obsolete full-model
     datamodule modules with no compatibility-only re-export files.
   - The new shared split lifecycle requires `train/val/test` split folders.
     This matches current active experiment datasets. Any remaining flat-layout
     support should be handled as a phase 9 deprecation decision rather than
     preserved in the unified base.

9. [Completed] Split shared instance behavior from Toy Mask2Former-specific
   behavior.

   The current `lfm/all_models/inst_seg/instance_dataset.py` and
   `lfm/all_models/inst_seg/instance_datamodule.py` are useful shared pieces,
   but their default output contract is still strongly Toy Mask2Former-shaped:

   - `pixel_values`
   - `mask_labels`
   - `class_labels`
   - `instance_mask`

   Refactor the instance task layer so the shared base owns image/label loading
   and instance-mask preparation, while target-format subclasses own:

   - Mask2Former target formatting
   - object-detection target formatting
   - Graha/TerraTorch object-detection dictionary shape

   Completed:

   - `lfm/all_models/inst_seg/instance_dataset.py` now separates:

     - `LunarInstanceMaskDataset`: shared instance-id mask loading,
       image preprocessing, nodata handling, normalization, crop, optional
       crater-box metadata, and mask shifting
     - `InstanceSegmentationDataset`: Mask2Former-specific output formatting
       for the Toy Mask2Former path
     - `ObjectDetectionInstanceSegmentationDataset`: object-detection target
       formatting with `boxes`, `labels`, `masks`, and `target_box_format`

   - `lfm/all_models/inst_seg/instance_datamodule.py` now separates:

     - `InstanceSegmentationDataModule`: existing Mask2Former-oriented default
       used by Toy instance datamodules
     - `InstanceMaskSegmentationDataModule`: image/mask instance-label output
     - `ObjectDetectionInstanceSegmentationDataModule`: object-detection output
       with object-detection collate policy

   - Existing Toy instance datamodules still import the same class names and
     preserve their current output contracts.

10. [Completed] Move Graha instance dataset/datamodule classes onto the shared
    instance base.

    Convert the current Graha instance classes from:

    - the old Graha instance datamodule file

    into Graha-specific specializations under the shared instance structure.

    Preserve:

    - TerraTorch ObjectDetectionTask target contract
    - crater box extraction from `.npz` metadata when present
    - fallback box/mask/label generation from instance-id masks
    - `target_box_format`
    - score-threshold-compatible prediction/cache behavior
    - nodata and normalization behavior

    Completed:

    - added `lfm/full_model/inst_seg/graha_instance_datamodule.py`
    - added `GrahaInstanceSegmentationDataModule` for Graha image/mask instance
      samples
    - added `GrahaObjectDetectionInstanceDataModule` for TerraTorch
      ObjectDetectionTask targets
    - preserved Graha constructor conventions such as `crop_size`, `means`,
      `stds`, `target_box_format`, `no_data_replace`, `mask_shift`, and
      split-folder limits while reusing the shared instance datamodule lifecycle
    - updated metadata crater boxes to apply `mask_shift` before crop, matching
      the old Graha `_finalize_sample` behavior

11. [Completed] Update adapters, workflows, scripts, notebooks, and audits to the
    new import paths.

    Active code should import the new all-model/task/model-family classes
    directly. Do not leave compatibility-only modules in
    `lfm/full_model/<task>`.

    Update references in:

    - Graha model adapters/components
    - Toy model adapters/components if class names move
    - workflow modules under `lfm/all_models/<task>/workflows`
    - checkpoint sweep and comparison plotting scripts
    - data behavior audit notebook/script utilities
    - active notebooks only when they are still maintained

    Completed:

    - Graha instance dependency loading now imports
      `GrahaObjectDetectionInstanceDataModule` from
      `lfm.full_model.inst_seg.graha_instance_datamodule`
    - instance Graha workflow, checkpoint sweep, checkpoint comparison plotting,
      and target-contract audit now use the new dependency key/import path
    - the maintained instance fine-tuning notebook dependency key was updated
    - data behavior audit utilities now compare the old base-branch
      `LunarObjectDetectionInstanceMaskDatamodule` against the new OOP-branch
      `GrahaObjectDetectionInstanceDataModule`
    - `lfm/full_model/inst_seg/__init__.py` no longer eagerly imports the old
      datamodule module

    Notes:

    - The old Graha instance datamodule file was deleted in substep 12 after
      active imports moved to the new Graha-specific module.
    - Scratch notebook references to the old module are left for notebook/archive
      cleanup rather than adding broad notebook churn to this implementation
      slice.

    Follow-up structural cleanup completed:

    - `lfm/all_models/sem_seg` now keeps model-neutral code under:

      - `data/`
      - `plotting/`
      - `testing/`
      - `workflows/`

    - `lfm/all_models/inst_seg` now keeps model-neutral code under:

      - `data/`
      - `plotting/`
      - `prediction/`
      - `testing/`
      - `workflows/`

    - Active imports were updated to the new subpackage paths. Top-level
      `lfm.all_models.sem_seg` and `lfm.all_models.inst_seg` package exports
      remain only for stable class/function API access, not as compatibility
      modules for old file paths.

12. [Completed] Delete obsolete full-model datamodule modules after the reference
    scan is clean.

    Deleted modules:

    - the old Graha instance datamodule module
    - the removed full-model all-task datamodule package files

    These were deleted only after active code stopped importing them. They were
    not replaced with compatibility-only re-export files.

    Completed:

    - active code now uses
      `lfm.full_model.inst_seg.graha_instance_datamodule.GrahaObjectDetectionInstanceDataModule`
      for Graha instance data
    - removed the obsolete Graha instance datamodule module
    - removed the unused full-model all-task datamodule base line:

      - `lfm/full_model/all_tasks/datamodules/lunar_segmentation_dataset.py`
      - `lfm/full_model/all_tasks/datamodules/lunar_segmentation_datamodule.py`
      - `lfm/full_model/all_tasks/datamodules/__init__.py`

    Deliberate retention:

    - `lfm/full_model/sem_seg/semantic_mask_datamodule.py` and
      `lfm/full_model/sem_seg/semantic_from_instance_datamodule.py` remain
      because they are Graha-specific semantic specializations that now inherit
      from the shared all-model semantic base. They should be considered for a
      clearer Graha-specific filename in phase 10 naming cleanup, not deleted
      as obsolete compatibility files.

13. [Completed] Rename confusing base classes once behavior is unified.

    The previous shared split datamodule base became the single active lunar
    split datamodule base and has been renamed to
    `LunarSegmentationDataModule`.

    Completed:

    - renamed
      the base class in
      `lfm/all_models/all_tasks/data/base_datamodule.py` to
      `LunarSegmentationDataModule`
    - updated semantic and instance shared datamodule inheritance
    - updated package exports from `lfm.all_models.all_tasks.data`
    - no compatibility alias was left behind

14. [In progress] Validate behavior preservation.

    Local checks completed for substeps 12-13 and the all-models subpackage
    cleanup:

    - formatted changed Python modules with the micromamba dev Python
    - ran Ruff on changed Python modules
    - compiled changed Python modules with the micromamba dev Python
    - verified key public imports for the shared lunar datamodule, semantic
      datamodule, instance datamodule, and Graha object-detection datamodule
    - validated edited notebook JSON
    - ran `git diff --check`

    No active shell wrapper remains in this substep's changed shell surface
    after deleting the obsolete legacy fine-tuning wrappers.

    Required behavior checks before closing phase 8:

    - semantic data behavior audit
    - instance data behavior audit
    - semantic smoke test
    - instance smoke test
    - checkpoint/prediction cache smoke check if imports moved there

Exit criteria for completing phase 8:

- There is one active shared dataset/datamodule base line under
  `lfm/all_models`.
- Toy and Graha task datamodules inherit from the same shared task bases unless
  a documented architecture constraint makes composition clearer.
- `full_model/<task>/*_datamodule.py` modules are removed or documented as
  temporary blockers with specific remaining references.
- Active imports do not depend on compatibility-only re-export modules.

Phase 9: Deprecation

Create and execute a deprecation plan for legacy features, scripts, modules, and
classes that no longer fit the OOP/Lightning workflow direction.

Status: partially complete and deferred. The deprecation inventory has been
created, one compatibility-only wrapper has been removed, and the remaining
cleanup candidates are lower priority than implementing the TerraTorch /
Mask R-CNN instance segmentation path.

Goal:

- Remove old paths only after references are updated.
- Do not leave compatibility-only files.
- Preserve active experiment behavior.

Sub-steps:

1. [Completed] Create a deprecation inventory under `docs/`.

   Candidate categories:

   - old Toy non-Lightning training drivers
   - obsolete notebook-only helpers
   - duplicated plotting/cache/evaluation utilities
   - old flat-layout data loading support, if pre-split datasets are now the
     intended contract
   - misleading compatibility files or imports left from previous moves

2. For each candidate, classify it as:

   - remove now
   - keep temporarily with a documented reason
   - keep as active supported behavior

3. Before deleting anything, run a reference scan across `lfm`, `scripts`,
   `notebooks`, and docs.

4. Remove references to deprecated code directly.

   Update imports, scripts, notebooks, and docs to the active package locations.
   Do not create compatibility shims.

5. Delete deprecated files once reference scans are clean.

6. Validate with import/syntax checks, structural scans, and smoke tests for the
   affected workflows.

Progress:

- Added `docs/segmentation_deprecation_inventory.txt`.
- Removed the compatibility-only shell wrapper:

  `scripts/shell/semantic_seg/sbatch_toy_sem_seg_comparison.sh`

  The active semantic comparison sbatch entrypoint is:

  `scripts/shell/semantic_seg/sbatch_semantic_seg_comparison.sh`

- Deferred removal of old non-Lightning Toy dataloader/stat helpers because the
  old toy training notebooks still reference them.
- Deferred Graha flat-layout support removal until there is an explicit
  decision that pre-split `train/val/test/{chips,labels}` is the only supported
  data contract.
- Remaining phase 9 cleanup is intentionally paused. Revisit it after the
  higher-priority instance segmentation architecture work, or when old Toy
  notebooks / flat-layout support become active blockers.

Phase 10: Naming Cleanup

Rename classes, functions, and modules whose names imply the wrong ownership
axis after the refactor. The largest planned rename is replacing repo-owned
`full_model` naming with `graha_model` / `Graha` naming.

Goal:

- Names should accurately communicate whether code is shared across tasks,
  shared across model families, Toy-specific, Graha-specific, semantic-specific,
  or instance-specific.
- `full_model` is legacy naming for repo-owned Graha code. New code should use
  `graha_model`, `Graha`, `graha_...`, and `--graha-...` as appropriate.
- Rename directly and update all references. Do not leave compatibility files.

Sub-steps:

1. Rename the repo-owned Graha package namespace from `lfm/full_model` to
   `lfm/graha_model`.

   Scope:

   - Move repo-owned Graha modules under `lfm/graha_model`.
   - Update imports in `lfm`, `scripts`, notebooks, and docs directly.
   - Preserve the vendored `graha-lunar-fm` directory name unless a separate
     vendor-repo rename is explicitly planned.
   - Do not create compatibility modules under `lfm/full_model`.

2. Rename code symbols and file names that still use `full_model` or
   "full-model" for Graha-owned behavior.

   Examples:

   - package/module paths should use `graha_model`
   - classes should use `Graha...`
   - functions and variables should use `graha_...`
   - CLI flags should use `--graha-...`
   - docs should say `Graha` or `Graha/Lunar-FM`

3. Audit class/module names that still imply only one task or model family when
   they are now shared.

   Candidate examples:

   - base dataset/datamodule names that should include `Base`
   - classes with `Toy`, `Graha`, `Semantic`, or `Instance` in the name despite
     broader ownership
   - modules whose filenames imply Graha ownership but now contain
     all-model behavior

4. Prioritize names that actively confuse workflow usage or inheritance.

   Avoid churn for names that are technically imperfect but isolated and not on
   active user-facing paths.

5. Rename in small batches by ownership axis:

   - all-task shared names
   - semantic shared names
   - instance shared names
   - Toy-specific names
   - Graha-specific names

6. Update imports and references directly.

7. Remove obsolete files after each rename batch.

8. Validate with formatting, linting, compile checks, reference scans, and smoke
   tests where behavior-bearing code was renamed.

Deferred Decisions
------------------

Do not move Toy workflow modules into the Graha package.

The desired model-family symmetry is not that Toy code lives under
`lfm/full_model` or future `lfm/graha_model`. The desired symmetry is:

- reusable Toy components live under `lfm/toy_model/{task}`
- reusable Graha components live under `lfm/graha_model/{task}` after the
  phase 10 rename
- shared Toy/Graha behavior lives under `lfm/all_models/{task}`
- importable workflow orchestration lives under `lfm/all_models/{task}/workflows`
- `scripts/python/{task}` contains executable entry points only

This is clearer than putting Toy implementation under the Graha package, and
clearer than keeping script-like finetuning orchestration under package
modules.

Do not start with a full experiment framework rewrite.

The safer path is bottom-up:

1. Shared data IO.
2. Shared normalization and nodata policy.
3. Shared base dataset/datamodule.
4. Toy semantic migration.
5. Mirrored semantic/instance data migration.
6. Model adapters.
7. Experiment runners.

Validation Expectations
-----------------------

Each phase should include:

- Import/syntax checks for touched modules.
- Focused unit or smoke tests where available.
- A structural scan for sem/inst and Toy/Graha symmetry.
- No compatibility-only files left behind.
